import { BusSeatBookingModel } from './bus-seat-booking-model';

describe('BusSeatBookingModel', () => {
  it('should create an instance', () => {
    expect(new BusSeatBookingModel()).toBeTruthy();
  });
});
