import { BookingResponseModel } from './booking-response-model';

describe('BookingResponseModel', () => {
  it('should create an instance', () => {
    expect(new BookingResponseModel()).toBeTruthy();
  });
});
